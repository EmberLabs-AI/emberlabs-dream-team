{
  "name": "dream-team",
  "version": "0.2.0",
  "description": "Ember Labs Dream Team — peer discovery and messaging across Claude Code + Codex CLI, single machine or cross-machine over Tailscale",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "broker": "bun broker.ts",
    "server": "bun server.ts",
    "codex:mcp": "bun codex-server.ts mcp",
    "codex:desktop-mcp": "bun codex-server.ts desktop-mcp",
    "codex:desktop-sidecar": "bun codex-server.ts desktop-sidecar",
    "codex:proxy": "bun codex-server.ts proxy",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
